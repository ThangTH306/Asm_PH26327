@model IEnumerable<Asm_PH26327.Models.Color>

@{
    ViewData["Title"] = "Colors";
}

<h1>Colors</h1>

<p>
    <a asp-action="CreateCol">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
           @* <th>
                @Html.DisplayNameFor(model => model.ColorId)
            </th>*@
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ma)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
          @*  <td>
                @Html.DisplayFor(modelItem => item.ColorId)
            </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ma)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.ActionLink("Edit", "Editms", new { id = item.ColorId }) |
                    @Html.ActionLink("Details", "Detailsms", new { id = item.ColorId }) |
                    @Html.ActionLink("Delete", "Deletems", new { id = item.ColorId })
            </td>
        </tr>
}
    </tbody>
</table>
